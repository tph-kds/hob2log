# hob2log

Implementation has started in [web/](web).

## Run the app

1. Go to [web/](web)
1. Copy [web/.env.example](web/.env.example) to `.env.local`
1. Start development server:

```bash
npm run dev
```